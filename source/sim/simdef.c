

#include "simvars.h"
#include "../sys/utils.h"

void queuesimres()
{
#if 0
	float prefab[FABX][FABX];
	int x, y;
return;
	g_pa[0].pos.x = 0;
	g_pa[0].pos.y = 0;
	g_pa[0].pos.z = 0;
	g_pa[0].mass = 10;

	g_pa[1].pos.x = -10;
	g_pa[1].pos.y = 0;
	g_pa[1].pos.z = 0;
	g_pa[1].mass = 20;

	for(x=0; x<FABX; ++x)
	{
		for(y=0; y<FABX; ++y)
		{
			
		}
	}
#endif
}